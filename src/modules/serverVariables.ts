import { Token, AuthenticationErrorMessage } from "./tokenManagement";

export const port: number = 5000;
export const token: Token = {token: 'token'};
export const errorMessage: AuthenticationErrorMessage = {errorMessage: 'Invalid user data'};