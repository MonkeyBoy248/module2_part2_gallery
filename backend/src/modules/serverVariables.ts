import { TokenObject, AuthenticationErrorMessage } from "./tokenInterface";

export const token: TokenObject = {token: 'token'};
export const errorMessage: AuthenticationErrorMessage = {errorMessage: 'Invalid user data'};